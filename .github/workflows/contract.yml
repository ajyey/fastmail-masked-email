name: Fastmail contract

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: fastmail-contract
  cancel-in-progress: false

jobs:
  contract:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v5
      - name: Use Node.js 24
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: 'npm'
      - name: Installing dependencies
        run: npm ci
      - name: Testing Fastmail contract
        env:
          JMAP_TOKEN: ${{ secrets.JMAP_TOKEN }}
          JMAP_HOSTNAME: ${{ secrets.JMAP_HOSTNAME }}
        run: npm run test:integration
